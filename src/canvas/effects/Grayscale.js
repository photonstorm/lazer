import ProcessPixels from 'canvas/ProcessPixels.js';

export default function Grayscale (imageData) {

}

    // greyScale (x, y, r, g, b, a) {

    //     let avg = (r + g + b) / 3;

    //     r = avg;
    //     g = avg;
    //     b = avg;

    //     return [ r, g, b, a ];

    // }
