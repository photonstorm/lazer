
export default function SquareDuty (
        {
            value = 0,
            ramp = 0
        } = {}
    ) {

    return {

        value,        // Square Duty
        ramp       // Duty Sweep

    };
    
}