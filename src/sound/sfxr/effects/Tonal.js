
export default function Tonal (
        {
            mod = 0,
            speed = 0
        } = {}
    ) {

    return {

        mod,        // Change amount
        speed       // Change speed

    };
    
}