
export default function Vibrato (
        {
            strength = 0,
            speed = 0
        } = {}
    ) {

    return {

        strength,   // Vibrato depth
        speed       // Vibrato speed

    };
    
}