let vec4 = Float32Array;

export default function (a, dst = new vec4(4)) {

    // var rcp = VMath.reciprocal;

    // dst[0] = rcp(a[0]);
    // dst[1] = rcp(a[1]);
    // dst[2] = rcp(a[2]);
    // dst[3] = rcp(a[3]);

    return dst;

}

